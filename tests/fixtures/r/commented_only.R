# library(tidymodels)
# set.seed(123)
# data_split <- initial_split(data, strata = target)
# data_recipe <- recipe(target ~ ., data = train_data)

print("All tidymodels references are in comments only")
